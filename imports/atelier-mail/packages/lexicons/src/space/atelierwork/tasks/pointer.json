{
  "lexicon": 1,
  "id": "space.atelierwork.tasks.pointer",
  "revision": 1,
  "description": "Private pointer to an Atelier Tasks resource used for cross-app linking.",
  "metadata": {
    "atelierStorage": "private-kv",
    "privacy": "Task pointers are private until the Permissioned Data model is finalized."
  },
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["taskUri", "createdAt"],
        "properties": {
          "taskUri": { "type": "string" },
          "createdAt": { "type": "string", "format": "datetime" }
        }
      }
    }
  }
}
