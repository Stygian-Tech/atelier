[build]
builder = "RAILPACK"
buildCommand = "bun run build --filter=@stygian/atelier-home-web"
watchPatterns = ["/apps/web/home/**", "/packages/**", "/package.json", "/bun.lock", "/turbo.json", "/infra/railway/services/home.toml"]

[deploy]
startCommand = "bun run --cwd apps/web/home start --hostname 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
