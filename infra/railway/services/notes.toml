[build]
builder = "DOCKERFILE"
dockerfilePath = "/apps/web/notes/Dockerfile"
watchPatterns = ["/apps/web/notes/**", "/packages/**", "/package.json", "/bun.lock", "/turbo.json", "/.dockerignore", "/infra/railway/services/notes.toml"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
