[build]
builder = "RAILPACK"
buildCommand = "bun run build --filter=@stygian/atelier-notes-web"
watchPatterns = ["/apps/web/notes/**", "/packages/**", "/package.json", "/bun.lock", "/turbo.json", "/infra/railway/services/notes.toml"]

[deploy]
startCommand = "bun run --cwd apps/web/notes start --hostname 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
