[build]
builder = "DOCKERFILE"
dockerfilePath = "/apps/web/mail/Dockerfile"
watchPatterns = ["/apps/web/mail/**", "/packages/**", "/package.json", "/bun.lock", "/turbo.json", "/.dockerignore", "/infra/railway/services/mail.toml"]

[deploy]
startCommand = "bun run --cwd apps/web/mail start --hostname 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
